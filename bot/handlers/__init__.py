"""
Aiogram router handlers for the Kinolar olami bot.

Submodules:
  start        — /start command and deep-link processing
  main_menu    — Reply keyboard button handlers
  search       — Text search and numeric code lookup
  movie        — Movie card, quality selection, rating, favorites, share
  profile      — User profile and favorites list
  subscription — Subscription-check callback
  errors       — Global error handler
"""
